// tabs/transcript.js — transcript tab: fully static placeholder, no data load.

window.StudentTabs = window.StudentTabs || {};

window.StudentTabs.transcript = {
    // Static content (the monolith never loaded anything for this section).
    init() {}
};
