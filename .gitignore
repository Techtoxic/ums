# Environment Variables
.env
.env.local
.env.production

# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Uploads
uploads/
!uploads/.gitkeep

# Logs
logs/
*.log

# OS Files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# AI coding assistants (per-developer local config; do not commit)
.claude/
.cursor/

# Build outputs
dist/
build/
.cache/

# Database dumps (but keep Drizzle migration files)
*.sql
*.dump
!drizzle/migrations/**/*.sql

# PM2
.pm2/

# Temporary files
tmp/
temp/

# Scratch diffs / audit artifacts (must never be committed - see SEV-C-003)
temp_diff.txt
*.diff

# SEV-H-015: one-time seeded admin credentials file (read once, then delete)
.seed-credentials.txt

# Real staff/trainer PII — seeded locally, NEVER committed (lawsuit/PII risk).
# Ignore the directory CONTENTS (not the dir itself) so the .example template
# can still be re-included; git cannot re-include a file under an ignored dir.
drizzle/private/*
!drizzle/private/*.example.json

# Markdown reports and other docs — keep README.md tracked, ignore everything else
*.md
!README.md
